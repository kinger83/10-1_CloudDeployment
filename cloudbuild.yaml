# Cuild Docker Image
steps:
  - name: "gcr.io/cloud-builders/docker"
    args:
      [
        "build",
        "-t",
        "us-west1-docker.pkg.dev/gcr.io/${PROJECT_ID}/calculator:$COMMIT_SHA,
        ".",
      ]

# Docker Push
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/${PROJECT_ID}/calculator:$COMMIT_SHA']

  

  

